# Configurable path (not hardcoded in classes sources) to users' avatars location on application server (or location on host machine),
# here relative path to application server's storage root in 'target/liberty/wlp/usr/servers/deafultServer'
uploadAvatarPath=./uploaded/users/avatar/